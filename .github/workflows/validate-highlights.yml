name: Validate highlights.json

on:
  pull_request:
    paths:
      - public/highlights.json

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Parse highlights.json
        run: python -m json.tool public/highlights.json > /dev/null
